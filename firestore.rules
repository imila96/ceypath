rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read so the site loads the same catalog everywhere; writes require Firebase Auth (admin).
    match /settings/catalog {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
